name: Render keep-alive

on:
  schedule:
    # Every 12 minutes
    - cron: "*/12 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: |
          curl -fsS --max-time 30 \
            "https://forecast-pipeline-v2.onrender.com/api/health" \
            || curl -fsS --max-time 30 \
            "https://forecast-pipeline-v2.onrender.com/api/health/ready"
